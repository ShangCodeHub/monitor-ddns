# 域名监控功能说明

## 📋 功能概述

定时检测配置的域名是否可访问，如果域名无法访问，自动发送短信告警。

## ⚙️ 配置文件

在 `config.env` 中添加以下配置：

```env
# 域名监控配置
MONITOR_ENABLED=true                    # 是否启用监控（true/false）
MONITOR_INTERVAL=300                    # 检测间隔（秒），默认300秒（5分钟）
MONITOR_URLS=http://nas.ritual-edu.com/,http://sci-z.ritual-edu.com/  # 要监控的域名（逗号分隔）
MONITOR_TIMEOUT=10                      # 连接超时时间（秒）
```

## 🚀 使用方法

### 1. 安装依赖

```bash
pip install requests
```

或安装所有依赖：

```bash
pip install -r requirements.txt
```

### 2. 启动定时监控

```bash
# 启动定时监控（每5分钟检测一次）
python ipScan.py --monitor
```

### 3. 立即检测一次（不启动定时监控）

```bash
# 立即检测一次所有域名
python ipScan.py --check
```

### 4. 发送短信（原有功能）

```bash
# 发送短信
python ipScan.py --phone 13800138000 --code 1234
```

## 📝 配置说明

### MONITOR_URLS（域名列表）

支持多个域名，用逗号分隔：

```env
MONITOR_URLS=http://nas.ritual-edu.com/,http://sci-z.ritual-edu.com/,http://example.com/
```

**注意**：
- 域名需要包含协议（http:// 或 https://）
- 多个域名用逗号分隔
- 后续添加新域名只需在配置文件中添加即可

### MONITOR_INTERVAL（检测间隔）

- 单位：秒
- 默认：300（5分钟）
- 建议：不要设置太短，避免频繁检测

### MONITOR_TIMEOUT（超时时间）

- 单位：秒
- 默认：10秒
- 如果域名在超时时间内无响应，视为不可访问

## 🔔 告警机制

### 告警触发条件

- 域名无法访问时（连接失败、超时、HTTP错误等）
- **只在状态从正常变为异常时发送短信**（避免重复发送）

### 告警短信内容

短信模板参数：
- `code`: "告警"
- `content`: "域名 {url} 无法访问，错误：{error_msg}"

### 状态记录

- 程序会记录每个域名的状态
- 只有从正常变为异常时才发送短信
- 域名恢复后，下次异常时会再次发送

## 📊 运行示例

### 启动监控

```bash
$ python ipScan.py --monitor

域名监控已启动，检测间隔: 300 秒
按 Ctrl+C 停止监控
[2026-02-03 16:30:00] 开始检测 2 个域名...
✓ http://nas.ritual-edu.com/ - 正常
✓ http://sci-z.ritual-edu.com/ - 正常
[2026-02-03 16:35:00] 开始检测 2 个域名...
✓ http://nas.ritual-edu.com/ - 正常
✗ http://sci-z.ritual-edu.com/ - 异常: 连接失败
[2026-02-03 16:35:01] 告警短信已发送: http://sci-z.ritual-edu.com/
```

### 立即检测

```bash
$ python ipScan.py --check

[2026-02-03 16:30:00] 开始检测 2 个域名...
✓ http://nas.ritual-edu.com/ - 正常
✓ http://sci-z.ritual-edu.com/ - 正常
```

## 🔧 添加新域名

只需在 `config.env` 文件的 `MONITOR_URLS` 中添加新域名：

```env
MONITOR_URLS=http://nas.ritual-edu.com/,http://sci-z.ritual-edu.com/,http://new-domain.com/
```

保存后，重启监控程序即可生效。

## ⚠️ 注意事项

1. **requests 模块**：需要安装 `requests` 模块才能使用监控功能
2. **短信费用**：每次域名异常都会发送短信，注意短信费用
3. **状态记录**：程序重启后状态记录会重置
4. **网络环境**：确保运行程序的服务器可以访问要监控的域名

## 🛠️ 故障排查

### 问题：提示 "requests模块未安装"

**解决**：
```bash
pip install requests
```

### 问题：监控不工作

**检查**：
1. `MONITOR_ENABLED` 是否为 `true`
2. `MONITOR_URLS` 是否配置正确
3. 域名格式是否正确（需要包含 http:// 或 https://）

### 问题：频繁发送短信

**解决**：
- 检查域名是否真的有问题
- 增加 `MONITOR_INTERVAL` 间隔时间
- 检查网络连接是否稳定
